function [i, j] = gpuProofs(ii, N)

i = floor((ii-1) / N) + 1;
j = mod(ii-1, N) + 1;
c =cos(i);

for n = 1:3000
    a = sqrt(i);
    b = atan2(i, j);
    a = sqrt(i);
    b = atan2(i, j);
    a = sqrt(i);
    b = atan2(i, j);
    a = sqrt(i);
    b = atan2(i, j);
    a = sqrt(i);
    b = atan2(i, j);
    a = sqrt(i);
    b = atan2(i, j);
    a = sqrt(i);
    b = atan2(i, j);
    a = sqrt(i);
    b = atan2(i, j);
    a = sqrt(i);
    b = atan2(i, j);
    a = sqrt(i);
    b = atan2(i, j);
    a = sqrt(i);
    b = atan2(i, j);
    a = sqrt(i);
    b = atan2(i, j);
end

end

